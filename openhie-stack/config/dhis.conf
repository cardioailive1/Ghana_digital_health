connection.dialect = org.hibernate.dialect.PostgreSQLDialect
connection.driver_class = org.postgresql.Driver
connection.url = jdbc:postgresql://dhis2-db:5432/dhis2
connection.username = dhis
connection.password = CHANGE_ME_dhis
# Server base URL (behind your reverse proxy)
server.base.url = http://localhost:8083
# Encryption password (REQUIRED, >=24 chars) — change this
encryption.password = change_me_change_me_change_me_24
